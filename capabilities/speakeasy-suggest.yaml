naftiko: 1.0.0-alpha2
info:
  label: Speakeasy API — Suggest
  description: 'Speakeasy API — Suggest. 4 operations. Lead operation: Generate Generic Suggestions for a List of Items. Self-contained
    Naftiko capability covering one Speakeasy business surface.'
  tags:
  - Speakeasy
  - Suggest
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SPEAKEASY_API_KEY: SPEAKEASY_API_KEY
capability:
  consumes:
  - type: http
    namespace: speakeasy-suggest
    baseUri: https://api.prod.speakeasy.com
    description: Speakeasy API — Suggest business capability. Self-contained, no shared references.
    resources:
    - name: v1-suggest-items
      path: /v1/suggest/items
      operations:
      - name: suggestitems
        method: POST
        description: Generate Generic Suggestions for a List of Items
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-suggest-openapi
      path: /v1/suggest/openapi
      operations:
      - name: suggestopenapi
        method: POST
        description: (DEPRECATED) Generate Suggestions for Improving an OpenAPI Document
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: x-session-id
          in: header
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-suggest-openapi-namespace_name-revision_reference
      path: /v1/suggest/openapi/{namespace_name}/{revision_reference}
      operations:
      - name: suggestopenapiregistry
        method: POST
        description: Generate Suggestions for Improving an OpenAPI Document Stored in the Registry
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: x-session-id
          in: header
          type: string
          required: true
        - name: namespace_name
          in: path
          type: string
          required: true
        - name: revision_reference
          in: path
          type: string
          description: Tag or digest
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-suggest-openapi_from_summary
      path: /v1/suggest/openapi_from_summary
      operations:
      - name: suggest
        method: POST
        description: Generate Suggestions for Improving an OpenAPI Document
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: x-session-id
          in: header
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SPEAKEASY_API_KEY}}'
  exposes:
  - type: rest
    namespace: speakeasy-suggest-rest
    port: 8080
    description: REST adapter for Speakeasy API — Suggest. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/suggest/items
      name: v1-suggest-items
      description: REST surface for v1-suggest-items.
      operations:
      - method: POST
        name: suggestitems
        description: Generate Generic Suggestions for a List of Items
        call: speakeasy-suggest.suggestitems
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/suggest/openapi
      name: v1-suggest-openapi
      description: REST surface for v1-suggest-openapi.
      operations:
      - method: POST
        name: suggestopenapi
        description: (DEPRECATED) Generate Suggestions for Improving an OpenAPI Document
        call: speakeasy-suggest.suggestopenapi
        with:
          x-session-id: rest.x-session-id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/suggest/openapi/{namespace-name}/{revision-reference}
      name: v1-suggest-openapi-namespace-name-revision-reference
      description: REST surface for v1-suggest-openapi-namespace_name-revision_reference.
      operations:
      - method: POST
        name: suggestopenapiregistry
        description: Generate Suggestions for Improving an OpenAPI Document Stored in the Registry
        call: speakeasy-suggest.suggestopenapiregistry
        with:
          x-session-id: rest.x-session-id
          namespace_name: rest.namespace_name
          revision_reference: rest.revision_reference
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/suggest/openapi-from-summary
      name: v1-suggest-openapi-from-summary
      description: REST surface for v1-suggest-openapi_from_summary.
      operations:
      - method: POST
        name: suggest
        description: Generate Suggestions for Improving an OpenAPI Document
        call: speakeasy-suggest.suggest
        with:
          x-session-id: rest.x-session-id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: speakeasy-suggest-mcp
    port: 9090
    transport: http
    description: MCP adapter for Speakeasy API — Suggest. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: generate-generic-suggestions-list-items
      description: Generate Generic Suggestions for a List of Items
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: speakeasy-suggest.suggestitems
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: deprecated-generate-suggestions-improving-openapi
      description: (DEPRECATED) Generate Suggestions for Improving an OpenAPI Document
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: speakeasy-suggest.suggestopenapi
      with:
        x-session-id: tools.x-session-id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: generate-suggestions-improving-openapi-document
      description: Generate Suggestions for Improving an OpenAPI Document Stored in the Registry
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: speakeasy-suggest.suggestopenapiregistry
      with:
        x-session-id: tools.x-session-id
        namespace_name: tools.namespace_name
        revision_reference: tools.revision_reference
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: generate-suggestions-improving-openapi-document-2
      description: Generate Suggestions for Improving an OpenAPI Document
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: speakeasy-suggest.suggest
      with:
        x-session-id: tools.x-session-id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
